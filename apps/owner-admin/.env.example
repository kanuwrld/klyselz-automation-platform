# Copy to .env.local for local dev. In Vercel, add same keys under Project Settings.
DATABASE_URL=""

NEXT_PUBLIC_CLIENT_NAME="KLYSELZ Admin"
NEXT_PUBLIC_ADMIN_URL="https://admin.klyselz.com"

# Generate strong value:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=""

# Webhook token for POST /api/leads from n8n/Make/assistants.
LEADS_WEBHOOK_SECRET=""
